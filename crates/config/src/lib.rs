//! Phase 1 typed TOML configuration loader for the LMAX-style MEV engine.
//!
//! Scaffold placeholder. The `Config` aggregate, `NodeConfig` /
//! `ObservabilityConfig` / `JournalConfig` sub-configs, and `ConfigError`
//! land in the next commit per the Batch A execution note Commit Grouping
//! step 3 (`feat(config): Config + sub-configs + ConfigError + 3 tests`).
