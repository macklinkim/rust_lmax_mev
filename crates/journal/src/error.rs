//! `JournalError` — unified error type for [`crate::journal::FileJournal`] and
//! [`crate::snapshot::RocksDbSnapshot`].
//!
//! Gate 3 scaffold placeholder. The 15 variants and their `thiserror` /
//! `#[non_exhaustive]` attributes land during Gate 5 implementation per spec
//! section B.4 (error matrix) and spec section 5.3 (no `#[from]` on the two
//! bincode variants).
